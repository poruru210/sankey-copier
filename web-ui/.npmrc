# pnpm configuration for Windows compatibility and standalone output

# Use hoisted mode (flat structure like npm) to resolve symlink issues
# This is essential for Next.js standalone output as it properly bundles dependencies
# Without this, styled-jsx and other transitive dependencies won't be found at runtime
node-linker=hoisted

# Reduce virtual store directory name length from default 120 to 60
# This prevents "path too long" errors during Inno Setup installer compilation
virtual-store-dir-max-length=60
