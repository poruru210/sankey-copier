# Production environment configuration for SANKEY Copier Web UI
#
# This file contains default values for production builds.
# The installer will override these values via NSSM environment variables.

# Rust Server API endpoint
# Default: http://localhost:3000
# This URL is used by Next.js rewrites to proxy API calls
NEXT_PUBLIC_API_URL=http://localhost:3000
